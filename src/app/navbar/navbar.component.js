import React, {Component} from 'react';

class Navbar extends Component {
    constructor(props) {
        super(props)
        {

        }
    }

    render() {
        const message = 'Navbar works';
        return (
            <div>Hello {message}</div>
        );
    }
}

export default Navbar;

